# world-sim

## TODOs
* figsize=(7.5, 7.5)
* in debug_plot.py line 245 should be:
  * if (sel.entity_type == EntityType.TRACKED_VEHICLE and sel._waypoints) or uav_one_point:
  * and not
  * if sel.entity_type == (EntityType.TRACKED_VEHICLE and sel._waypoints) or uav_one_point:
* 

## check
* 

## add to PRD
* ...